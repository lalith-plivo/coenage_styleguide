---
group: 'cestyle-core'
package: '@cestyle/core'
title: Loader
order: 1
slug: /core/loader/
category: 'feedback'
description: 'Indicate loading state'
props: ['Loader']
import: "import { Loader } from '@cestyle/core';"
source: 'cestyle-core/src/components/Loader/Loader.tsx'
docs: 'core/Loader.mdx'
styles: ['Loader', 'single-node']
---

import { LoaderDemos } from '@cestyle/demos';

## Usage

By default, loader will be rendered with `theme.primaryColor`.
You can choose any color defined in [theme.colors](/theming/extend-theme/#default-colors):

<Demo data={LoaderDemos.configurator} />

## Configure default loader

You can configure default loader in [CeStyleProvider](/theming/cestyle-provider/),
it will be used in Loader and [LoadingOverlay](/core/loading-overlay/) components by default:

```tsx
import { CeStyleProvider } from '@cestyle/core';

function Demo() {
  return (
    <CeStyleProvider theme={{ loader: 'bars' }}>
      <YourApp />
    </CeStyleProvider>
  );
}
```

## Size

Size controls loader height or width (depends on loader variant). Loader has predefined sizes: xs, sm, md, lg, xl.
Alternatively, you can use a number to set width or height in px:

```tsx
<Loader size="sm" /> // -> predefined small size
<Loader size={50} /> // -> size in px
```
