import { StepCompleted } from './StepCompleted';

describe('@cestyle/core/StepCompleted', () => {
  it('has correct displayName', () => {
    expect(StepCompleted.displayName).toStrictEqual('@cestyle/core/StepCompleted');
  });
});
